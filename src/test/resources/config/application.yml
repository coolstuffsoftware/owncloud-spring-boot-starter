spring:
  jackson:
    serialization:
      indent-output: true
logging:
  level:
    root: info
    software.coolstuff.springframework.owncloud: debug
owncloud:
  test:
    velocity:
      properties:
        input.encoding: UTF8
        resource.load: classpath
        classpath.resource.loader.class: org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader
        classpath.resource.loader.path: /velocity